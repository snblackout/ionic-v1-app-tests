(function () {
    'use strict';

    var moduleName = 'app.core',
            requires = [
                'ionic',
                'ngStorage',
                'ngCordova'
            ];

    angular.module(moduleName, requires);

})();
